{
  "name": "aiia-ctms-audit-anchor",
  "version": "1.0.0",
  "description": "Solidity contract + Hardhat deployment for the AIIA CTMS blockchain audit-anchor layer (Section 8).",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
